[build]
  command     = "npm run build"
  publish     = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  # Zone geometry batch-fetching on first cold refresh needs extra time.
  # After the first refresh the zone cache is warm and this limit is never hit.
  timeout = 26
