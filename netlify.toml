[build]
  command     = "npm run build"
  publish     = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Set VITE_API_URL in the Netlify UI after you deploy the backend:
  # Site settings → Environment variables → Add: VITE_API_URL = https://your-backend.railway.app
